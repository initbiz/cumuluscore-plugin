<?php namespace {{author}}\{{name}}\Classes;

use {{author}}\{{name}}\Plugin;
use InitBiz\CumulusCore\Classes\GuardBase;

class Guard extends GuardBase
{

    /**
     * Guard constructor.
     */
    public function __construct()
    {
        $plugin = new Plugin(app());
        $this->moduleSlug = str_slug(\Lang::get($plugin->pluginDetails()['name']));
    }
}
