# ===================================
#  Relation Behavior Config
# ===================================

clusters:
    label: 'initbiz.cumuluscore::lang.cluster.cluster'
    view:
        list: $/initbiz/cumuluscore/models/cluster/columns.yaml
        toolbarButtons: add|remove
        showSearch: true
    manage:
        list: $/initbiz/cumuluscore/models/cluster/columns.yaml
    pivot:
        form: $/initbiz/cumuluscore/models/cluster/fields.yaml
